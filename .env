##########################################
# 🌐 FRONTEND ENVIRONMENT CONFIG
# AlphaForge - Production Build (No Supabase)
##########################################

# === Backend API Base URL ===
# Must match NGINX reverse proxy → backend:8080
VITE_BACKEND_URL="https://api.alphaforge.skillsifter.in"
VITE_WS_URL=wss://api.alphaforge.skillsifter.in
VITE_DEBUG=false

# === Frontend App Origin ===
# Must match backend CORS whitelist
FRONTEND_ORIGIN="https://alphaforge.skillsifter.in"

# === UI / Branding Flags ===
VITE_APP_NAME="AlphaForge"
VITE_ENVIRONMENT="production"
VITE_ENABLE_ANALYTICS="true"

##########################################
# ⚠️ SECURITY NOTES:
# - Do NOT add ICICI API keys, secrets, DB credentials,
#   JWT secrets, or any sensitive backend config.
# - Frontend `.env` is PUBLIC and visible in browser.
# - ICICI credentials are only stored backend-side.
##########################################
